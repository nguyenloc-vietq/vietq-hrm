version: "3.9"

services:
  hr-api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: hr-api
    ports:
      - "3592:3592"         
    env_file:
      - ../.env              
    volumes:
      - ../src/uploads:/app/src/uploads   
    restart: always